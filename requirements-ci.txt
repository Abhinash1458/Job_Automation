# Lean deps for the GitHub Actions daily run (no browser automation / web app —
# the cloud job only scrapes, scores, and writes the report; you apply manually).
openai>=1.40
python-dotenv>=1.0
apify-client>=1.7
requests>=2.32
beautifulsoup4>=4.12
pypdf>=5.0
python-docx>=1.1
openpyxl>=3.1
rich>=13.7
